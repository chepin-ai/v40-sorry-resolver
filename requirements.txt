# v40 sorry resolver — dependency manifest (major versions pinned).
#
# Runtime
openai>=2.46,<3        # AsyncOpenAI client for all LLM providers
httpx>=0.28,<1         # SorryDB remote client (lazy import) + openai transport
#
# Test
pytest>=9.1,<10
pytest-asyncio>=1.4,<2
#
# Optional: experimental LeanDojo verification path (flag-gated, SPEC 3.7).
# Install only when --verifier dojo is used:
#   lean-dojo>=4.20,<5
#   GitPython>=3.1,<4
#
# The real verification path also needs a Lean 4 toolchain on PATH
# (elan + lake, see README.md "本地运行"); it is NOT a pip dependency.
